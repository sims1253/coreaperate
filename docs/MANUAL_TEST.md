# Two-instance acceptance checklist — NOT YET EXECUTED

Record the REAPER version, OS, Python version, instrument settings, network type, date, and both project/IPC paths. Do this with disposable copies. Keep exports and logs locally. Never infer a real-REAPER pass from a Lua mock or Python test.

1. **Bootstrap.** Run `probe.lua` in an empty disposable project; read `reaper/probe-result.txt`. Run `smoke.lua` in another empty disposable project; read `runtime/smoke-result.txt`. The smoke script uses `runtime/` and refuses to overwrite its baseline JSON: move previous smoke artifacts into a dated archive before repeating. Prepare the demo using README instructions, save, close, and copy into A/B local files. Start host, both companions, and both bridges with different identities/IPC folders. Verify equal canonical exports before editing. Repeat save/close/reopen to check metadata persistence.
2. **Simultaneous owned tracks.** A claims Drums and B claims Bass. Both create notes while the other edits. Exercise pitch, onset, duration, velocity, channel, and note mute. Verify the owner/revision display and pending indicator, then compare exports. Contend for the same unclaimed track: only one claim may win. Release and hand over after pending edits drain; old-owner proposals must fail.
3. **Items.** Use `create_item.lua` or verified non-looping native insertion. Move an item, right-trim to hide notes, extend it again, and verify those notes remain. Delete it; compare both exports. Copy an ordinary item while retaining its original: verify new collaboration item/take IDs. Try pooling, cross-track moves, left trim, take FX, source offset, CC, SysEx, and text in separate disposable branches. Each unsupported edit must pause visibly and leave the local data intact. Ambiguous copied identity must pause.
4. **Independent workspaces.** Set different play/edit cursors, loop/time selections, zoom/scroll, editor windows, item/track/note selections, mute/solo, record arm and monitoring. Edit supported musical data remotely. Record exactly what stays independent. Verify unchanged note selection; record behavior for changed, duplicate, and ambiguous notes. Test playing both instances during edits and listen for glitches. Switch project tabs while the bridge runs and verify only its bound project changes; close the bound project and confirm safe stop.
5. **Delivery/restart faults.** First export both sides. Stop a companion and edit its owned track; publication must pause, with no automatic merge on reconnect. Restart the coordinator with its existing DB; owner reservations remain and epochs increase. Stop/restart companions and bridges, query old operation outcomes, and exercise replay. For file-reordering tests, pause the receiving bridge, copy selected inbound JSON files into a local archive, delay their appearance, then resume; never alter payload IDs or use Syncthing. Duplicate an already archived event under a fresh transport filename; it must not reapply. Do not overwrite a live message file. Compare exports after explicit reconciliation.
6. **Unauthorized edit.** A edits B's Bass track. Before inbound changes can overwrite it, A must pause. Export and import the live branch into an empty project to verify the unsaved notes survive. In A's paused project, deliberately restore accepted data after backup/confirmation, then reconnect. Try unsupported project structure and confirm project-wide pause.
7. **Undo.** Both users make accepted edits. A invokes scoped undo on the track containing A's latest musical operation. B's accepted state must remain unchanged. Make a subsequent same-track revision or handover; A's inverse must be refused. Try native Undo/Redo and record observed behavior honestly: owned supported changes may appear as new edits, unowned changes must pause, and no universal native-Undo interception is promised.
8. **Equality and timing.** Press Export on both sides once pending edits drain. Compare with:

   ```powershell
   .\.venv\Scripts\python -m coreaperate compare C:/local/A/recovery/EXPORT.json C:/local/B/recovery/EXPORT.json
   ```

   Equality compares canonical `current`, excluding UI state. Record edit-settle → receiver-observation timing for at least 20 changes; report median/max and network topology. Record playback disruption, not just data convergence. The initial target is under a second on localhost/LAN, not a claimed result.

For each step record **pass/fail/unverified**, the exact action, exports, and discrepancies in a dated local report. The current delivery has no measured two-instance latency or complete manual acceptance result.
